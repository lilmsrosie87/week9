// was overriding before correction .class in html, but still not working. Trying to add an output so hopefully it will show
function colorChange() {
	document.getElementById("cotDamn").backgroundColor = "#cc99ff";
}

document.write(colorChange())

// stopped at #6